# Téléphone physique (même Wi‑Fi que le PC) — IP actuelle du PC :
EXPO_PUBLIC_API_URL=http://192.168.100.23:8001/api

# Émulateur Android : http://10.0.2.2:8001/api
# PC seul / Expo web : http://127.0.0.1:8001/api
#
# Backend obligatoire :
#   cd backend && php artisan serve --host=0.0.0.0 --port=8001
# (sans 0.0.0.0 le téléphone ne peut pas joindre l’API)
